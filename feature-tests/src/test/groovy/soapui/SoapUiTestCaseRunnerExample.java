package soapui;

/**
 * Note: currently does not work
 */
public class SoapUiTestCaseRunnerExample {

    public static void main(String[] args) throws Exception {
     /*
        String[] properties = {"orgnr=" + 974720760};
        SoapUITestCaseRunner testCaseRunner = new SoapUITestCaseRunner(new SoapUiTestCaseRunnerExample().getClass().getSimpleName());
        testCaseRunner.setProjectFile(new SoapUiTestCaseRunnerExample().getClass().getResource("noarkExchange-soapui-project.xml").toExternalForm());
        testCaseRunner.setTestSuite("noarkExchangeBinding TestSuite");
        testCaseRunner.setTestCase("GetCanReceiveMessage TestCase");
        testCaseRunner.setProjectProperties(properties);
        testCaseRunner.runRunner();
        */
    }
}
