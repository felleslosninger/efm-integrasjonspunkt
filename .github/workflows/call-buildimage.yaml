name: CI Workflow

on:
  workflow_dispatch:
  push:
  pull_request:
    types: [ ] # Empty list ensures PRs are not triggered. Trigger nytt bygg..

jobs:
  #also deploys to maven central/ossrh
#  build-install-and-deploy-libs:
#    uses: felleslosninger/github-workflows/.github/workflows/ci-maven-install-deploy-lib.yml@efm-tilpasninger-3
#    with:
#      java-version: 8
#      sbom-path: integrasjonspunkt/target
#      fetch-depth: 0
#    secrets: inherit
    
  call-workflow-image-build-publish:
#      uses: felleslosninger/github-workflows/.github/workflows/ci-spring-boot-build-publish-image.yml@til-bundy
      needs: [build-install-and-deploy-libs]
      with:
        application-path: integrasjonspunkt/
        image-name: integrasjonspunkt
        java-version: 8
        slack-channel-id: C05NLQCFR41
        update-versions: "false"
        fetch-depth: 0
      secrets: inherit  
  