app:
  logger:
    destination: qa-stream-meldingsutveksling.difi.no:443
spring:
  cloud:
    config:
      enabled: 'true'
      uri: https://qa-meldingsutveksling.difi.no/discovery/config
      fail-fast: 'true'
