app:
  logger:
    destination: qa-stream-meldingsutveksling.difi.no:443
spring:
  cloud:
    config:
      enabled: true
      uri: https://eformidling.dev/discovery/eureka/config
      fail-fast: true
